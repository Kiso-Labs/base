# Base specifications

Specifications describe the durable product behavior Base must provide. Feature work updates the relevant specification before implementation, and `plans/` records the bounded work used to bring the code into conformance.

Specs declare observable rules and ownership boundaries. Implementation details, temporary sequencing, and validation commands belong in plans or code.
