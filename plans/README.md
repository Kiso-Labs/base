# Base implementation plans

Plans are the temporal bridge from accepted specifications to merged code. Each file owns one bounded implementation scope and declares its dependencies and validation criteria in frontmatter; the frontmatter is the authoritative work graph.
